# صيدلية الحازم — تحليل المبيعات والديون

أداة ويب لتحليل ومقارنة بيانات المتابعة اليومية للصيدلية (المبيعات، الأرباح، الديون)، مبنية بـ React + TypeScript + Vite.

- **لوحة التحكم**: مؤشرات أداء (المبيعات، الربح، الديون، متوسط الفاتورة، نسبة الدليفري، المرتجعات)، رسم بياني لاتجاه المبيعات والأرباح، اتجاه الديون (آجل + معلق)، توزيع طرق الدفع، توزيع قيمة الفواتير، وقسم **مقارنة الفترات** (آخر ٧ أيام مقابل الـ٧ اللي قبلها، الشهر الحالي مقابل اللي فات، أو فترة مخصصة).
- **البيانات اليومية**: استيراد ملف CSV مُصدَّر من شيت المتابعة اليومية على جوجل شيت (الأعمدة تتعرّف تلقائيًا بالاسم العربي بغض النظر عن ترتيبها)، أو إضافة/تعديل/حذف يوم يدويًا.

البيانات تُخزَّن في `localStorage` بالمتصفح (بدون خادم)، وتبدأ ببيانات تجريبية عند أول تشغيل — استورد ملفك الحقيقي عشان يحل محلها.

## استيراد البيانات من جوجل شيت

من الشيت: **ملف > تنزيل > قيمة مفصولة بفواصل (.csv)**، ثم من صفحة "البيانات اليومية" اضغط **استيراد CSV** واختر الملف. زر "تنزيل نموذج الأعمدة" بيدّيك ملف فيه كل أسماء الأعمدة المتعرّف عليها كمرجع.

## التشغيل محليًا

```bash
npm install
npm run dev
```

## البناء للإنتاج

```bash
npm run build
```

النشر يتم تلقائيًا على GitHub Pages عبر GitHub Actions مع كل push.
